export class Contact{
  constructor(public id: number, public name: string, public description: string,
    public adress: string, public email: string, public  category: string, public phoneNumbers:[]
    ) {

}
}
